<UserControl x:Class="University_WPF.Components.UserControlInsertData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:University_WPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500"
             x:Name="UserControlInsert">  
        <UserControl.Resources>
            <ResourceDictionary Source="..\Resurces\Stylewindow.xaml"/>
        </UserControl.Resources>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding TitleUC}" Grid.Column="0" Grid.Row="1" Style="{StaticResource TextBlock_A}"/>
            <TextBox x:Name="InsertTextBox" Grid.Column="1" Grid.Row="1" Style="{StaticResource TextBox_A}"/>
        </Grid>
    </UserControl>
