<Window x:Class="University_WPF.Windows.StudentMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University_WPF.Windows"
        mc:Ignorable="d"
        Title="Меню Студентов" Height="850" Width="1000" WindowState="Maximized"
        Background="{StaticResource Background}">
    <Window.Resources>
        <ResourceDictionary Source="..\StyleWindow.xaml" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="ListStudent" Style="{StaticResource List}" Grid.Column="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding AccountName}" Width="100"/>                        
                        <TextBlock Text="{Binding AccountStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
